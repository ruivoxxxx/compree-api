import { Injectable } from '@nestjs/common';

@Injectable()
export class PedidosService {
    //usuario vai ser buscado na primeira query e logo apos vc vai atribuar a const para o data.usuaario, para dessa forma conseguir criar o pedido e o usuario estar relacionado ao pedido
}
